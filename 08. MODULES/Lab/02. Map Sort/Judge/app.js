const mapSort = require('./mapSort')

/* let map = new Map();

map.set(3, "Pesho");
map.set(1, "Gosho");
map.set(7, "Aleks");

map = mapSort(map, (a, b) => a[1].localeCompare(b[1]));

console.log(map); */

result.mapSort = mapSort